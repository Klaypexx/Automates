dispatcher: dotnet8 # только так пока что
projectDirectory: Lab4 # путь от корня репозитория до директории, в которой находится CMakeLists.txt
target: Lab4 # таргет (он указан в CMakeLists.txt, например в "add_executable(mealy_moore_converter main.cpp)")
